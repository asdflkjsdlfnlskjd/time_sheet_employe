# Импорт/Экспорт Табеля - Руководство

## 📋 Обзор

Система теперь поддерживает полный импорт и экспорт данных табеля в CSV и XLSX форматы. Это позволяет:

- **Экспорт**: Сохранить табель в файл для резервной копии или редактирования в Excel
- **Импорт**: Загрузить отредактированный табель обратно в систему

## 🚀 Экспорт Табеля

### Как экспортировать:

1. Перейдите на страницу "Табель"
2. Выберите месяц и год (если нужны конкретные данные)
3. Если требуется, выберите отдел для фильтрации (доступно только для супер-админов)
4. Нажмите кнопку **"📥 Экспорт"**
5. Файл `timesheet_[месяц]_[год]_[дата].csv` будет загружен

### Формат экспортируемого файла:

```
Табель учета рабочего времени,Апрель,2026
(пустая строка)
Сотрудник,Табельный номер,Отдел,День 1,День 2,...,День 30,Рабочих дней,Всего часов
Иванов Иван Иванович,001,Отдел разработки,—,ОТ(8ч),...,—,28,224
...
```

### Коды статусов в экспорте:

| Код | Расшифровка | Значение |
|-----|-------------|----------|
| — | Присутствовал | Рабочий день |
| ОТ | Отсутствовал | День отсутствия |
| ОП | Опоздал | Поздний приход |
| РУ | Ранний уход | Ранний уход |
| ОТП | Отпуск | Дни отпуска |
| БО | Больничный | Дни болезни |
| ВЫХ | Выходной | Выходной день |

## 📤 Импорт Табеля

### Как импортировать:

1. Перейдите на страницу "Табель"
2. Нажмите кнопку **"📨 Импорт"**
3. В появившемся окне:
   - Выберите **месяц** для импорта
   - Выберите **год** для импорта
   - Выберите **файл** (CSV, TXT или XLSX)
4. Нажмите **"Импортировать"**
5. Система обновит данные в базе

### Подготовка файла для импорта:

1. **Экспортируйте** текущий табель из системы
2. **Откройте** файл в Excel или другом редакторе
3. **Отредактируйте** данные:
   - Измените количество часов
   - Измените статусы (используя коды из таблицы выше)
   - Добавьте новые записи (если нужно)
4. **Сохраните** файл как CSV или XLSX

### Примеры значений ячеек:

- `—` - присутствовал целый день
- `ОТ(8ч)` - отсутствовал, запись на 8 часов
- `8.5` - просто количество часов (будет интерпретирован как присутствие)
- `ОТП(0ч)` - день отпуска
- `БО(0ч)` - больничный день
- `-` (пусто) - нет данных

## ⚙️ Процесс Импорта

1. **Валидация**: Система проверяет формат файла
2. **Парсинг**: Извлекает данные по сотрудникам и дням
3. **Проверка доступа**: Обычные админы могут импортировать только данные своего отдела
4. **Сохранение**: Существующие данные за месяц перезаписываются новыми
5. **Отчет**: Выводится количество загруженных и пропущенных записей

## ⚠️ Важные Замечания

- **Обновление**: При импорте существующие данные за выбранный месяц будут **перезаписаны**
- **Доступ**: Обычные администраторы могут работать только с данными своего отдела
- **Ошибки**: Некорректные записи пропускаются, система выдает список ошибок
- **Резервная копия**: Рекомендуется перед импортом экспортировать текущие данные

## 🔄 Рекомендуемый Рабочий Процесс

### Редактирование табеля в Excel:

```
1. Экспорт → получить файл из системы
2. Открыть в Excel
3. Редактировать данные (месячные корректировки, отпуска и т.д.)
4. Сохранить как CSV/XLSX
5. Импорт → загрузить отредактированный файл
```

### Массовое обновление:

```
1. Экспортировать данные
2. Использовать Excel функции (поиск/замена, формулы)
3. Импортировать обновленный файл
```

## 🆘 Диагностика Проблем

### Проблема: "Ошибка при валидации"
- Проверьте, что выбраны месяц и год
- Проверьте, что файл не пустой

### Проблема: "Сотрудник не найден"
- Убедитесь, что табельные номера совпадают с системой
- Проверьте правильность написания

### Проблема: "Нет доступа"
- Если вы обычный админ, вы можете редактировать только свой отдел
- Обратитесь к супер-администратору

### Проблема: "Данные не загружены"
- Проверьте формат файла (должен быть CSV, XLSX или TXT)
- Убедитесь, что в файле есть данные после строки заголовка
- Проверьте кодировку файла (должна быть UTF-8)

## 📊 Примеры Использования

### Пример 1: Добавление отпуска

```
Исходная строка:
Иванов Иван Иванович,001,Отдел,—(8ч),—(8ч),...

Отредактированная:
Иванов Иван Иванович,001,Отдел,—(8ч),ОТП(0ч),...
```

### Пример 2: Исправление ошибок

```
Было:
Петров Петр Петрович,002,Отдел,ОТ(8ч),—(8ч),...

Исправлено:
Петров Петр Петрович,002,Отдел,—(8ч),—(8ч),...
```

## 🔐 Безопасность

- Импорт проверяет права доступа пользователя
- Логируются все операции импорта/экспорта
- Файлы хранятся во временной папке и удаляются после обработки
- Система не позволяет импортировать данные по сотрудникам других отделов (для обычных админов)

## 📞 Поддержка

Если возникли проблемы с импортом/экспортом:
1. Проверьте формат файла
2. Убедитесь в правильности данных
3. Обратитесь к администратору системы
